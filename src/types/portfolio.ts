export interface DesktopIconData {
  id: string;
  label: string;
  bgPosition: string;
  windowId: string;
  transform: string;
}

export interface WindowContentData {
  id: string;
  title: string;
  icon: string;
}
